import type { FC } from "react";

const Hero: FC = function() {
  return (
    <div className="w-full h-screen text-9xl flex items-center justify-center">
      HERO
    </div>
  );
}

export default Hero;
